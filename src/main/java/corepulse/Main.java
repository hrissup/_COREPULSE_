package com.corepulse;

import javafx.application.Application; // To access the launch method

public class Main {

    public static void main(String args[]) {
// To Build the application thread and load up DashboardUI class to display window
        Application.launch(DashboardUI.class, args);
    }
}
